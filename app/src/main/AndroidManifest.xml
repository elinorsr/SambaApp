<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".core.MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SambaApp"
        tools:targetApi="31">
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/pink_border_button" />
        <!-- מסך בריאות -->
        <activity android:name=".user.HealthActivity" />
        <activity
            android:name=".user.SettingsActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".lessons.view.LessonDetailsActivity" />
        <activity android:name=".lessons.viewmodel.LessonViewActivity" />
        <activity android:name=".lessons.view.LessonListActivity" />
        <activity android:name=".lessons.view.AddLessonActivity" />
        <activity android:name=".lessons.view.EditLessonActivity" />

        <activity android:name=".media.VideoPlayerActivity" />

        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

</manifest>